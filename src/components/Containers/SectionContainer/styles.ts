import { StyleSheet } from "react-native";

export const sectionContainerStyles = StyleSheet.create({
  container: {
    width: "100%",
    paddingHorizontal: 20,
  },
  subContainer: {
    flex: 1,
    width: "100%",
    maxWidth: 800,
  },
});
