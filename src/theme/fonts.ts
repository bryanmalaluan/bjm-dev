import { scaleFont } from "@src/utils/dataFormat";

export default {
  size: {
    small: scaleFont(12),
    regular: scaleFont(16),
    large: scaleFont(20),
    heading: scaleFont(30),
    subHeading: scaleFont(25),
  },
};
